# The Nexus Core - Requirements
# The system works in basic mode with ZERO dependencies.
# Install groups below to unlock specific features.

# --------------------------
# API SERVER (required to run nexus_core_api.py)
# --------------------------
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.9   # required for file uploads

# --------------------------
# VECTOR INDEXING & SEMANTIC SEARCH (optional)
# Enables vector-based semantic search and hierarchical indexing.
# --------------------------
llama-index>=0.9.0

# --------------------------
# MEMORY MANAGEMENT (optional)
# Enables advanced conversation memory features.
# --------------------------
langchain>=0.1.0

# --------------------------
# UI (required to run nexus_core_ui.py)
# --------------------------
streamlit>=1.32.0

# --------------------------
# LLM PROVIDERS (install whichever you use)
# --------------------------
# openai>=1.0.0       # for LLM_PROVIDER=openai
# anthropic>=0.25.0   # for LLM_PROVIDER=anthropic
# Ollama needs no Python package - just install Ollama itself.

# --------------------------
# CONFIG (optional)
# Better .env file parsing than the built-in fallback.
# --------------------------
# python-dotenv>=1.0.0

# --------------------------
# TESTING
# --------------------------
pytest>=9.0.0
pytest-mock>=3.14.0
httpx>=0.27.0          # required by FastAPI TestClient
